const int my_analog_pin = A0;
const int LED = 8;
int ADC_val = 0;
int digital_map_LED;
void setup()
{
  Serial.begin(115200);
  pinMode(LED, OUTPUT);
  pinMode(my_analog_pin, INPUT);
}

void loop()
{
